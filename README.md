# Basic-Port-Scanner
A simple Python-based network utility tool designed to scan specific ports on a local IP host to check their current connection status.
## Features
- Scans common network ports (21, 22, 80, 443, etc.) on a specified target IP.
- Uses Python's built-in `socket` library to establish quick TCP connections.
- Safe for execution in a local laboratory or localhost environment.
